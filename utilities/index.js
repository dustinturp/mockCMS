const newEmployee = require('./new-employee-prompt')
const newRole = require('./new-role-prompt');
const newDepartment = require('./new-department-prompt');
const mainPrompt = require('./main-prompt')

module.exports = {mainPrompt, newEmployee, newRole, newDepartment}
