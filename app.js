const {mainPrompt, newDepartment, newRolePrompt, newEmployee} = require('./utilities')
const cTable = require('console.table');
const mysql = require('mysql2')
//Start the main prompt


mainPrompt()
.then(promptResponse => {

})
